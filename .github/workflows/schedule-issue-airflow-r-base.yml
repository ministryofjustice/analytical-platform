---
name: 📥 Maintenance - Create Airflow R Base Issue

on:
  schedule:
    - cron: "0 8 1 * *" # UTC time: “At 08:00 on first day-of-month”

permissions: {}

jobs:
  create-maintenance-issue:
    name: Create Maintenance Issue
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v6.0.0
      - name: Create Maintenance Issue
        id: create_maintenance_issue
        run: |
          bash scripts/github/issue-creation/create-maintenance-issue.sh
        env:
          GH_TOKEN: ${{ secrets.MOJ_PROJECT_ANALYTICAL_PLATFORM_WRITE }}
          GH_REPO: ${{ github.repository }}
          TITLE: 📥 Maintenance - Airflow R Base Image
          ASSIGNEES:
          LABELS: maintenance
          BODY: |
            > [!NOTE]
            > This issue is created by a scheduled workflow.

            ### User Story

            As an… engineer on the Analytical Platform
            I need/want/expect to… keep the Airflow R Base image up to date
            So that… security / best practice etc

            ### Definition of Done
            - [ ] Merge any open dependabot Pull Requests in the repository (noted below).
            - [ ] In the [repository](https://github.com/ministryofjustice/analytical-platform-airflow-r-base), confirm that the [software](https://github.com/ministryofjustice/analytical-platform-airflow-r-base?tab=readme-ov-file#managing-software-versions) and [tests](https://github.com/ministryofjustice/analytical-platform-airflow-r-base/blob/main/test/container-structure-test.yml) are up-to-date.
            - [ ] Create a new [release](https://github.com/ministryofjustice/analytical-platform-airflow-r-base/releases).

          PINNED: false
          CLOSE_PREVIOUS: false
